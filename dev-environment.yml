# conda env create -f environment.yml
name: lume-model-dev
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pydantic
  - numpy
  - pyyaml
  - botorch

  # dev requirements
  - pytest
  - pre-commit
  - ruff

  # docs requirements
  - mkdocs
  - mkdocstrings
  - mkdocstrings-python
  - mkdocs-material
  - mkdocs-include-markdown-plugin
