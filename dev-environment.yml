# conda env create -f environment.yml
name: lume-model-dev
channels:
  - conda-forge
dependencies:
  - python>=3.9
  - pydantic>2.3
  - numpy
  - pyyaml
  - botorch<0.11

  # dev requirements
  - pytest
  - pre-commit
  - ruff

  # docs requirements
  - mkdocs
  - mkdocstrings
  - mkdocstrings-python
  - mkdocs-material
